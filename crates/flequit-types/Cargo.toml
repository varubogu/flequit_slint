[package]
name = "flequit-types"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }

# Error handling
thiserror = "2.0.16"
